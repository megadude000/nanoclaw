---
phase: 01-discord-channel-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/channels/discord.ts
  - src/channels/discord.test.ts
  - .env
  - data/env/env
autonomous: false
requirements: [CHAN-03]
user_setup:
  - service: discord
    why: "Bot token required for Discord gateway connection"
    env_vars:
      - name: DISCORD_BOT_TOKEN
        source: "Discord Developer Portal -> Applications -> Bot -> Reset Token"
    dashboard_config:
      - task: "Enable Message Content privileged intent"
        location: "Discord Developer Portal -> Applications -> Bot -> Privileged Gateway Intents"
      - task: "Generate bot invite URL and invite to server"
        location: "Discord Developer Portal -> Applications -> OAuth2 -> URL Generator (scopes: bot; permissions: Send Messages, Read Message History, View Channels)"

must_haves:
  truths:
    - "Bot logs shard disconnect events with warning level"
    - "Bot logs shard reconnecting events with info level"
    - "Bot logs shard resume events with info level and replayed event count"
    - "Tests verify shard lifecycle event handlers are registered"
    - "Bot token is configured in .env and synced to data/env/env"
  artifacts:
    - path: "src/channels/discord.ts"
      provides: "Shard lifecycle event handlers for reconnection observability"
      contains: "ShardDisconnect"
    - path: "src/channels/discord.test.ts"
      provides: "Tests for shard lifecycle logging"
      contains: "ShardDisconnect"
    - path: ".env"
      provides: "Discord bot token configuration"
      contains: "DISCORD_BOT_TOKEN"
  key_links:
    - from: "src/channels/discord.ts"
      to: "discord.js Events"
      via: "client.on(Events.ShardDisconnect/ShardReconnecting/ShardResume)"
      pattern: "Events\\.Shard(Disconnect|Reconnecting|Resume)"
    - from: "src/channels/discord.ts"
      to: "src/logger.ts"
      via: "logger.warn/info for shard events"
      pattern: "logger\\.(warn|info).*shard"
---

<objective>
Add shard lifecycle event logging to the DiscordChannel for reconnection observability (CHAN-03), write tests for it, then collect the Discord bot token from the user and configure the environment.

Purpose: discord.js handles reconnection internally, but without shard event listeners there is no visibility into disconnect/reconnect cycles. This enhancement makes CHAN-03 (automatic reconnection with observability) fully compliant. The bot token setup enables live verification.

Output: Enhanced discord.ts with shard logging, tests for it, configured bot token ready for live use.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-discord-channel-foundation/01-RESEARCH.md
@.planning/phases/01-discord-channel-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Shard event types from discord.js v14 -->
From discord.js Events enum (used in discord.ts):
```typescript
import { Events } from 'discord.js';
// Events.ShardDisconnect - (event: CloseEvent, shardId: number) => void
// Events.ShardReconnecting - (shardId: number) => void
// Events.ShardResume - (shardId: number, replayedEvents: number) => void
// Events.ShardReady - (shardId: number, unavailableGuilds: Set<string> | undefined) => void
// Events.ShardError - (error: Error, shardId: number) => void
```

From src/logger.ts (existing logger):
```typescript
import logger from '../logger.js';
// logger.warn({ shardId, code }, 'message')
// logger.info({ shardId, replayedEvents }, 'message')
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shard lifecycle logging and tests for CHAN-03</name>
  <read_first>
    - src/channels/discord.ts (current implementation after merge -- find the connect() method where shard handlers must be added)
    - src/channels/discord.test.ts (current test structure -- understand mocking patterns to add shard lifecycle tests)
    - src/logger.ts (logger import path and usage pattern)
  </read_first>
  <files>src/channels/discord.ts, src/channels/discord.test.ts</files>
  <behavior>
    - Test: When client emits ShardDisconnect event, logger.warn is called with { shardId, code } and message containing 'disconnected'
    - Test: When client emits ShardReconnecting event, logger.info is called with { shardId } and message containing 'reconnecting'
    - Test: When client emits ShardResume event, logger.info is called with { shardId, replayedEvents } and message containing 'resumed'
    - Test: All three shard event handlers are registered during connect()
  </behavior>
  <action>
    **RED phase -- write tests first:**

    Add a new describe block to `src/channels/discord.test.ts` for shard lifecycle events. Follow the existing test mocking patterns in the file. The tests should:

    1. Verify that after `connect()` is called, the client has `.on()` handlers registered for `Events.ShardDisconnect`, `Events.ShardReconnecting`, and `Events.ShardResume`
    2. Verify that emitting `ShardDisconnect` with `{ code: 1006 }` and shardId `0` calls `logger.warn` with object containing `{ shardId: 0, code: 1006 }` and string containing `'disconnected'`
    3. Verify that emitting `ShardReconnecting` with shardId `0` calls `logger.info` with object containing `{ shardId: 0 }` and string containing `'reconnecting'`
    4. Verify that emitting `ShardResume` with shardId `0` and replayedEvents `5` calls `logger.info` with object containing `{ shardId: 0, replayedEvents: 5 }` and string containing `'resumed'`

    Run tests -- they should FAIL (red).

    **GREEN phase -- implement:**

    In `src/channels/discord.ts`, inside the `connect()` method, add these three event handlers AFTER the client is created but BEFORE `this.client.login(this.botToken)`:

    ```typescript
    this.client.on(Events.ShardDisconnect, (event, shardId) => {
      logger.warn({ shardId, code: event.code }, 'Discord shard disconnected');
    });
    this.client.on(Events.ShardReconnecting, (shardId) => {
      logger.info({ shardId }, 'Discord shard reconnecting');
    });
    this.client.on(Events.ShardResume, (shardId, replayedEvents) => {
      logger.info({ shardId, replayedEvents }, 'Discord shard resumed');
    });
    ```

    Run tests -- they should PASS (green).
  </action>
  <verify>
    <automated>cd /home/andrii-panasenko/nanoclaw && npx vitest run src/channels/discord.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ShardDisconnect' src/channels/discord.ts` returns at least 1
    - `grep -c 'ShardReconnecting' src/channels/discord.ts` returns at least 1
    - `grep -c 'ShardResume' src/channels/discord.ts` returns at least 1
    - `grep 'Discord shard disconnected' src/channels/discord.ts` returns a match
    - `grep 'Discord shard reconnecting' src/channels/discord.ts` returns a match
    - `grep 'Discord shard resumed' src/channels/discord.ts` returns a match
    - `grep -c 'ShardDisconnect' src/channels/discord.test.ts` returns at least 1
    - `npx vitest run src/channels/discord.test.ts` exits with code 0
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Shard lifecycle event handlers added to connect() method, tests written and passing, build succeeds</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Configure Discord bot token and verify live connection</name>
  <files>.env, data/env/env</files>
  <read_first>
    - .claude/skills/add-discord/SKILL.md (Phase 3 setup instructions for token creation)
    - .env.example (verify DISCORD_BOT_TOKEN entry exists from merge)
  </read_first>
  <action>
    After user provides the Discord bot token:
    1. Add `DISCORD_BOT_TOKEN=<user-provided-token>` to `.env`
    2. Sync environment: `mkdir -p data/env && cp .env data/env/env`
    3. Build: `npm run build`
    4. Restart service: `systemctl --user restart nanoclaw`
    5. Check logs for "Discord bot connected" message: `tail -20 logs/nanoclaw.log`

    If user types "skip", mark token setup as deferred -- all code changes are already complete and tested.
  </action>
  <what-built>
    Discord channel integration is fully merged and enhanced with reconnection observability logging. The bot needs a token to connect to your Discord server.
  </what-built>
  <how-to-verify>
    I need you to create a Discord bot and provide the token. Here are the steps:

    1. Go to the Discord Developer Portal: https://discord.com/developers/applications
    2. Click "New Application" and name it (e.g., "Andy Assistant")
    3. Go to the "Bot" tab on the left sidebar
    4. Click "Reset Token" to generate a new bot token -- copy it immediately (shown only once)
    5. Under "Privileged Gateway Intents", enable:
       - Message Content Intent (required to read message text)
       - Server Members Intent (optional, for member display names)
    6. Go to OAuth2 > URL Generator:
       - Scopes: select "bot"
       - Bot Permissions: select "Send Messages", "Read Message History", "View Channels", "Manage Channels"
       - Copy the generated URL and open it in your browser to invite the bot to your server
    7. Paste the bot token here

    After you provide the token, I will:
    - Add DISCORD_BOT_TOKEN to .env
    - Sync to data/env/env for the container
    - Build and restart NanoClaw
    - Verify the bot appears online in your Discord server
  </how-to-verify>
  <verify>
    <automated>grep -c 'DISCORD_BOT_TOKEN' /home/andrii-panasenko/nanoclaw/.env 2>/dev/null || echo 0</automated>
  </verify>
  <acceptance_criteria>
    - `.env` contains a line matching `DISCORD_BOT_TOKEN=` with a non-empty value
    - `data/env/env` contains a line matching `DISCORD_BOT_TOKEN=`
    - If not skipped: `logs/nanoclaw.log` contains "Discord bot connected"
  </acceptance_criteria>
  <resume-signal>Paste your Discord bot token, or type "skip" to defer token setup (code changes are already complete)</resume-signal>
  <done>Discord bot token configured in .env and data/env/env, bot appears online in Discord server (or setup deferred by user)</done>
</task>

</tasks>

<verification>
1. `grep 'Events.ShardDisconnect' src/channels/discord.ts` returns a match
2. `grep 'Events.ShardReconnecting' src/channels/discord.ts` returns a match
3. `grep 'Events.ShardResume' src/channels/discord.ts` returns a match
4. `npx vitest run src/channels/discord.test.ts` all tests pass
5. `npx vitest run` full suite passes
6. `npm run build` succeeds
7. (After token setup) Bot appears online in Discord server
</verification>

<success_criteria>
- Shard lifecycle events (ShardDisconnect, ShardReconnecting, ShardResume) are logged with appropriate log levels
- Tests cover all three shard event handlers
- All tests pass including new shard lifecycle tests
- Build succeeds
- Discord bot token is configured (or deferred by user choice)
</success_criteria>

<output>
After completion, create `.planning/phases/01-discord-channel-foundation/01-02-SUMMARY.md`
</output>
