---
phase: 17-search-mcp-tools
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/cortex/cortex-mcp-tools.test.ts
autonomous: true
requirements:
  - SEARCH-01
  - SEARCH-02
  - SEARCH-03
  - MCP-01
  - MCP-02
  - MCP-03

must_haves:
  truths:
    - "A failing test exists that describes cortex_search embedding the query and querying Qdrant with correct filter params"
    - "A failing test exists that describes hybrid routing: a vault path query is routed to direct file read, not Qdrant"
    - "A failing test exists that describes the confidence firewall blocking L20 writes when no medium+ L10 entries exist"
    - "A failing test exists that describes cortex_read returning file content and blocking path traversal"
    - "A failing test exists that describes cortex_write validating frontmatter and writing an IPC file"
    - "All tests import the tool logic module from the correct path and fail with module-not-found or assertion errors (RED)"
  artifacts:
    - path: "src/cortex/cortex-mcp-tools.test.ts"
      provides: "Unit test scaffold for all Phase 17 requirements"
      min_lines: 120
  key_links:
    - from: "src/cortex/cortex-mcp-tools.test.ts"
      to: "src/cortex/cortex-mcp-tools.ts"
      via: "import"
      pattern: "import.*cortex-mcp-tools"
---

<objective>
Create the failing test scaffold for the three Cortex MCP tool logic functions.

Purpose: Nyquist compliance — tests must go RED before implementation. This plan establishes the full test coverage contract before any production code is written, so Plan 02 can implement to make them GREEN.

Output: `src/cortex/cortex-mcp-tools.test.ts` with 6+ failing tests covering all 6 requirement IDs in this phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-search-mcp-tools/17-CONTEXT.md
@.planning/phases/17-search-mcp-tools/17-RESEARCH.md

<!-- Existing test pattern to replicate -->
@src/cortex/embedder.test.ts
</context>

<interfaces>
<!-- Key contracts the test file must import against -->
<!-- These types will live in src/cortex/cortex-mcp-tools.ts (created in Plan 02) -->

The test file imports from a module that does NOT yet exist:
```typescript
import {
  buildSearchHandler,    // factory: returns the cortex_search handler fn
  buildReadHandler,      // factory: returns the cortex_read handler fn
  buildWriteHandler,     // factory: returns the cortex_write handler fn
  isVaultPath,           // pure helper: detects exact vault path vs natural language
  checkConfidenceFirewall, // pure helper: returns true if write should be blocked
} from './cortex-mcp-tools.js';
```

Mock targets (verified from RESEARCH.md):
- `@qdrant/js-client-rest` — mock `search()` and `scroll()` methods
- `openai` — mock `embeddings.create()` returning `{ data: [{ embedding: number[] }] }`
- `fs` (node:fs) — mock `existsSync()` and `readFileSync()`
- `./cortex-mcp-tools.js` will be the module under test
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing test scaffold for all 6 requirement IDs</name>
  <files>src/cortex/cortex-mcp-tools.test.ts</files>

  <read_first>
    - src/cortex/embedder.test.ts — exact vi.mock() + vi.hoisted() pattern used in this project
    - src/cortex/schema.ts — CortexFieldsStrict field names (cortex_level, confidence, domain, scope)
    - .planning/phases/17-search-mcp-tools/17-RESEARCH.md — Pattern 1-5 for exact behavior being tested
  </read_first>

  <behavior>
    MCP-01 / SEARCH-01 (hybrid routing — semantic path):
    - Input: query = "how does the IPC watcher work" (no .md suffix, not a vault path)
    - Expected: openai.embeddings.create() is called once, qdrant.search() is called with vector + filter
    - Expected return: array of objects with { path, score, level, domain, project }

    MCP-01 / SEARCH-01 (hybrid routing — exact vault path):
    - Input: query = "Areas/Projects/NanoClaw/architecture.md"
    - Expected: openai.embeddings.create() is NOT called, fs.readFileSync() is called with the resolved vault path
    - Expected return: file content string

    SEARCH-03 (filter params):
    - Input: query = "ipc design", project = "nanoclaw", cortex_level = "L10"
    - Expected: qdrant.search() called with filter.must containing two conditions: { key: 'project', match: { value: 'nanoclaw' } } and { key: 'cortex_level', match: { value: 'L10' } }

    MCP-02 (cortex_read — valid path):
    - Input: path = "Areas/Projects/NanoClaw/decisions.md"
    - Expected: fs.readFileSync() called with "/workspace/cortex/Areas/Projects/NanoClaw/decisions.md"
    - Expected return: { content: [{ type: 'text', text: <file content> }] }

    MCP-02 (cortex_read — path traversal blocked):
    - Input: path = "../../src/config.ts"
    - Expected: isError: true returned, fs.readFileSync() NOT called

    SEARCH-02 (confidence firewall — blocked):
    - Input: cortex_level = "L20", domain = "nanoclaw", qdrant.scroll() returns points = []
    - Expected: checkConfidenceFirewall returns true (write blocked)

    SEARCH-02 (confidence firewall — allowed):
    - Input: cortex_level = "L20", domain = "nanoclaw", qdrant.scroll() returns points = [{ id: 'abc' }]
    - Expected: checkConfidenceFirewall returns false (write allowed)

    SEARCH-02 (L10 always allowed — no firewall):
    - Input: cortex_level = "L10", domain = "any"
    - Expected: checkConfidenceFirewall returns false without calling qdrant.scroll()

    MCP-03 (cortex_write — valid frontmatter):
    - Input: path = "Areas/test.md", content = valid markdown with cortex_level, confidence, domain, scope in frontmatter
    - Expected: writeIpcFile mock called with { type: 'cortex_write', path: 'Areas/test.md' }
    - Expected return: success message, not isError

    MCP-03 (cortex_write — missing frontmatter fields):
    - Input: content = markdown with frontmatter missing "confidence" field
    - Expected: isError: true, writeIpcFile NOT called
  </behavior>

  <action>
    Create `src/cortex/cortex-mcp-tools.test.ts` using the vi.mock() + vi.hoisted() pattern from embedder.test.ts.

    Structure:
    1. vi.hoisted() block to set up mock factories for @qdrant/js-client-rest, openai, and node:fs
    2. vi.mock('@qdrant/js-client-rest', ...) — mock QdrantClient with search() and scroll() methods
    3. vi.mock('openai', ...) — mock OpenAI with embeddings.create() returning { data: [{ embedding: Array(1536).fill(0.1) }] }
    4. vi.mock('node:fs', ...) — mock existsSync() returning true, readFileSync() returning '---\ncortex_level: L10\n---\ntest content'
    5. Import { buildSearchHandler, buildReadHandler, buildWriteHandler, isVaultPath, checkConfidenceFirewall } from './cortex-mcp-tools.js'
    6. Write describe blocks per requirement ID with the behaviors listed above

    The import of './cortex-mcp-tools.js' will cause module-not-found error — this is the RED state.

    Key technical details:
    - Use vi.hoisted() for gray-matter mock if needed (same hoisting requirement as embedder.test.ts)
    - Mock the writeIpcFile function: since it will be internal to cortex-mcp-tools.ts, expose it via the build* factory pattern so tests can verify it was called
    - Use vi.fn() for all mock functions so .mock.calls can be inspected
    - Factory pattern: buildSearchHandler({ qdrant, openai, vaultRoot }) returns async (args) => result
      buildReadHandler({ vaultRoot }) returns async (args) => result
      buildWriteHandler({ qdrant, writeIpc, vaultRoot }) returns async (args) => result
    - isVaultPath(query: string): boolean — pure, no deps
    - checkConfidenceFirewall(level: string, domain: string, qdrant: QdrantClient): Promise<boolean>
  </action>

  <verify>
    <automated>npx vitest run src/cortex/cortex-mcp-tools.test.ts 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - File src/cortex/cortex-mcp-tools.test.ts exists: `ls src/cortex/cortex-mcp-tools.test.ts`
    - File has at least 10 test cases: `grep -c "it(" src/cortex/cortex-mcp-tools.test.ts` outputs >= 10
    - Import statement for cortex-mcp-tools.ts exists: `grep "from './cortex-mcp-tools.js'" src/cortex/cortex-mcp-tools.test.ts`
    - Test run fails with module-not-found or import error (RED state confirmed): `npx vitest run src/cortex/cortex-mcp-tools.test.ts 2>&1 | grep -E "Cannot find|FAIL|failed"`
    - All 6 requirement IDs covered: `grep -E "MCP-01|MCP-02|MCP-03|SEARCH-01|SEARCH-02|SEARCH-03" src/cortex/cortex-mcp-tools.test.ts` shows 6 matches
  </acceptance_criteria>

  <done>
    src/cortex/cortex-mcp-tools.test.ts exists with 10+ failing tests in RED state. Every requirement ID (SEARCH-01, SEARCH-02, SEARCH-03, MCP-01, MCP-02, MCP-03) has at least one test case. The test file imports from ./cortex-mcp-tools.js which does not yet exist.
  </done>
</task>

</tasks>

<verification>
Run: npx vitest run src/cortex/cortex-mcp-tools.test.ts
Expected: Tests FAIL with module-not-found or similar (RED state — correct for Wave 0)
</verification>

<success_criteria>
- Test scaffold committed in RED state
- All 6 requirement IDs (SEARCH-01, SEARCH-02, SEARCH-03, MCP-01, MCP-02, MCP-03) have test cases
- Factory pattern (buildSearchHandler/buildReadHandler/buildWriteHandler) clearly defined in tests
- No production code written in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/17-search-mcp-tools/17-01-SUMMARY.md`
</output>
